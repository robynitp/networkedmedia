var servi = require('servi');

var app = new servi(true);


serveFiles("public");

route('/api', serveAPI);
function serverAPI(request) {
  //request.serveFile("json/weather.json");
}


if (typeof run === 'function') {
  app.defaultRoute(run);
}
start();